#include <iostream>
#include <vector>
#include <string>
#include <stdexcept>

using namespace std;

// codificando usando cosidog conolucional
vector<string> convolutional_enconde(const string &input){
    vector<string> result;
    // code that
    return result;
}

int main(int arg, char *argv[]){
    return 0;
}